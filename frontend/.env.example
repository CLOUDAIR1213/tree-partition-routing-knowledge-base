# Leave empty when using the Vite development proxy.
VITE_API_BASE_URL=

# Tree-index development UI. Keep this distinct from the legacy UI on 5173.
VITE_DEV_PORT=5174

# Vite proxy target used by `npm run dev`.
VITE_PROXY_TARGET=http://127.0.0.1:8001
